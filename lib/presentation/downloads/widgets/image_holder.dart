import 'package:flutter/material.dart';

class ImageHolderDownloads extends StatelessWidget {
  const ImageHolderDownloads({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
